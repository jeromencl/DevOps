apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: DevOps
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5 
  template:
    metadata:
      labels:
        app: DevOps
    spec:
      containers:
      - name: pleungtestapp
        image: jeromeacr.azurecr.io/jeromencl/DevOps:$BUILD_ID
        ports:
        - containerPort: 16824
---
apiVersion: v1
kind: Service
metadata:
  name: DevOps
spec:
  type: LoadBalancer
  ports:
  - port: 30080
  selector:
    app: DevOps
